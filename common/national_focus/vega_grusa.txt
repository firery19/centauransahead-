shared_focus = {
		id = VEGRS_reunification
		icon = GFX_goal_generic_allies_build_infantry
		x = 40
		y = 0
		cost = 10
		search_filters = {   }
		
		available_if_capitulated = yes

		completion_reward = {
			army_experience = 25
			add_doctrine_cost_reduction = {
				name = land_doc_bonus
				cost_reduction = 0.5
				uses = 1
				category = land_doctrine
			}
		}
	}

	shared_focus = {
		id = VEGRS_reaching_out
		icon = GFX_goal_generic_small_arms
		prerequisite = { focus = VEGRS_reunification }
		x = 0
		y = 1
		relative_position_id = VEGRS_reunification
		cost = 10

		search_filters = {   }
		
		available_if_capitulated = yes

		completion_reward = {
		if = {
			limit = {
			tag = VEG
			}
			VEG = { country_event = aqu.1 }
		}
		else_if = {
			limit = {
				tag = GRS
			}
			GRS = { country_event = ori.1 }
			}
		}
	}